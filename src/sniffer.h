#ifndef _SNIFFER_
#define _SNIFFER_
#include <semaphore.h>
#include <pcap.h>

static inline int sniff_process() __attribute__((always_inline));

static inline int process_pcap() __attribute__((always_inline));

static inline int sniff_process()
{

}

static inline int process_pcap(char *filename)
{
  
}
